# Local path to private datasets (never commit actual data)
DATA_DIR=/path/to/local/private/data

# Optional private filenames
SYS_ORDER_FILE=private_sys_order.xlsx
GEO_FILE=private_geo.xlsx

# Optional key for geocoding if used
GOOGLE_MAPS_KEY=
